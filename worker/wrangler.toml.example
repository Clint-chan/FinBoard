name = "market-board-api"
main = "index.js"
compatibility_date = "2024-01-01"

# KV 绑定 - 需要先创建 KV namespace
# 运行: npx wrangler kv namespace create CONFIG_KV
# 然后把返回的 id 填到下面
[[kv_namespaces]]
binding = "CONFIG_KV"
id = "你的KV_NAMESPACE_ID"
